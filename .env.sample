# Mobius Development Environment Configuration
# ============================================
# 
# IMPORTANT: Security Notice
# --------------------------
# This file contains sample configuration for development purposes.
# 
# For DEVELOPMENT:
# - Copy this file to .env for local development
# - The docker-compose.yml provides default values if .env is not present
# - Default password is 'mobius_dev_password' - ONLY use for local development
#
# For PRODUCTION:
# - ALWAYS use strong, unique passwords
# - NEVER commit .env files with real credentials to version control
# - Use proper secret management (e.g., Kubernetes secrets, AWS Secrets Manager)
# - Enable SSL/TLS for all database connections
#
# ============================================

# PostgreSQL Configuration
# ------------------------
# Database name
POSTGRES_DB=mobius

# Database user
POSTGRES_USER=mobius

# Database password
# SECURITY WARNING: Change this to a strong password!
# Default in docker-compose.yml: mobius_dev_password (DEVELOPMENT ONLY)
POSTGRES_PASSWORD=your_secure_password_here

# Note: The DATABASE_URL in docker-compose.yml is automatically constructed
# from the above PostgreSQL variables. No need to set it separately.

# Additional Security Recommendations:
# - Use a password manager to generate strong passwords
# - Rotate passwords regularly
# - Monitor database access logs
# - Implement proper backup and disaster recovery procedures