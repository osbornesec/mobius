"""
API Router for Mobius Platform v1

This module aggregates all API endpoints for version 1 of the platform.
"""

from fastapi import APIRouter

# Create the main API router
api_router = APIRouter()

# TODO: Include endpoint routers once they are implemented
# Example:
# from app.api.v1.endpoints import contexts, agents, analytics
# api_router.include_router(contexts.router, prefix="/contexts", tags=["contexts"])
# api_router.include_router(agents.router, prefix="/agents", tags=["agents"])
# api_router.include_router(analytics.router, prefix="/analytics", tags=["analytics"])
